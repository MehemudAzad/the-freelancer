# Payment Service Configuration
server.port=8087
spring.application.name=payment-service

# External Service URLs
auth.service.url=http://localhost:8081

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5437/payment_db
spring.datasource.username=admin
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration - Disabled for now since no migrations exist
spring.flyway.enabled=false
# spring.flyway.locations=classpath:db/migration
# spring.flyway.baseline-on-migrate=true

# Stripe Configuration
# Replace these with your actual Stripe keys from https://dashboard.stripe.com/apikeys
stripe.secret-key=${STRIPE_SECRET_KEY:sk_test_51P95ukD7fmYTjBJv6vUDgGWnSzJ7PBEvnjDv6R9meqOQUooxOcWlsatfEUpIDr9Uu3OrDh25kNA3MzStOkNWBVR900IrLtvMXB}
# For local development with Stripe CLI, use the secret from 'stripe listen' command output
stripe.webhook.endpoint-secret=${STRIPE_WEBHOOK_SECRET:whsec_QrjJBOwhMqDnenDKh9qf4RgJiklq6lc6}
stripe.platform-fee-percentage=5

# Logging Configuration
logging.level.com.thefreelancer.microservices.payment=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operationsSorter=method
springdoc.show-actuator=true
